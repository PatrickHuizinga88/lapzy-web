---
const { title, description } = Astro.props;
---

<div class="flex flex-col gap-4 bg-white/50 border border-border rounded-3xl p-8">
  <div class="flex items-center justify-center bg-slate-200 rounded-2xl size-20">
    <slot name="icon" />
  </div>
  <h2 class="text-3xl font-semibold mt-2">{ title }</h2>
  <p class="text-muted-foreground">{ description }</p>
</div>